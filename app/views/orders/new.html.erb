<h1>Shopping Cart</h1>
<table class="table table-striped table-hover">
  <thead class="thead-inverse">
    <th>Product</th>
    <th>Quantity</th>
    <th>Price</th>
    <th>Total</th>
  </thead>
  <tbody>
    <% @price_total = [] %>
    <% @shopping_cart.items.each do |product, quantity| %>
      <% product_total_price = Product.find(product).price * quantity %>
      <% @price_total << product_total_price  %>

          <tr>
            <td><%= Product.find(product).name %></td>
            <td><%= quantity %></td>
            <td><%= Product.find(product).price %></td>
            <td class="text-right"><%= number_to_currency(product_total_price, precision: 2, separator: ',', unit: '')%></td>
          </tr>
    <% end %>
    <thead>
      <td ><strong>Total</strong></td>
      <td></td>
      <td></td>
      <td class="text-right"><strong><%=number_to_currency(@order_total, precision: 2, separator: ',', unit: '€')%></strong></td>

    </thead>
  </tbody>
</table>

<%= link_to "Place order", user_orders_path, method: :post, class: "btn btn-success" %>
